<h1>id: <%= @task.id %> のタスク編集</h1>

<%= form_with(model: @task, local: true) do |f| %>
  <%= f.label :cotent, 'タスク名' %>
  <%= f.text_field :content %>
  
  <%= f.submit '再登録' %>
<% end %>

<%= link_to 'タスク一覧に戻る', task_path %>